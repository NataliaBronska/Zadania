#include <cmath>

double logarytm_naturalny(double liczba)
{
	return log(liczba);
}
double odchylenie_standardowe(double x, double y)
{
	return sqrt((x * x + y * y) / 2.0);
}
double exponent(double liczba)
{
	return exp(liczba);
}